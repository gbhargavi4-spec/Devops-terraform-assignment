global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: devops-app
    static_configs:
      - targets:
          - app:8000
    metrics_path: /metrics

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
